# 仓库分离完成检查清单

## ✅ 完成状态检查

### 1. jubianai-backend 仓库

- [ ] 在 GitHub 上创建了 `jubianai-backend` 仓库
- [ ] 本地 `jubianai` 目录已初始化 Git
- [ ] 已添加远程仓库（SSH）
- [ ] 代码已推送到 GitHub
- [ ] 可以在 GitHub 上看到代码

**检查命令**:
```bash
cd jubianai
git remote -v
# 应该显示: git@github.com:ZhehuanUnique/jubianai-backend.git
```

### 2. doubao-rag-service 仓库

- [ ] 在 GitHub 上创建了 `doubao-rag-service` 仓库
- [ ] 本地 `doubao-rag` 目录已初始化 Git
- [ ] 已添加远程仓库（SSH）
- [ ] 代码已推送到 GitHub
- [ ] 可以在 GitHub 上看到代码

**检查命令**:
```bash
cd doubao-rag
git remote -v
# 应该显示: git@github.com:ZhehuanUnique/doubao-rag-service.git
```

### 3. 主仓库更新

- [ ] `.gitignore` 已添加 `jubianai/` 和 `doubao-rag/`
- [ ] 更改已提交
- [ ] 已推送到主仓库

**检查命令**:
```bash
# 在主仓库根目录
cat .gitignore | Select-String "jubianai|doubao-rag"
# 应该显示这两行
```

## 🔍 验证步骤

### 验证 1: 检查 GitHub 仓库

访问以下 URL 确认代码已上传：
- https://github.com/ZhehuanUnique/jubianai-backend
- https://github.com/ZhehuanUnique/doubao-rag-service

### 验证 2: 测试独立服务

#### jubianai-backend

```bash
cd jubianai
# 检查是否可以独立运行
python -m uvicorn backend.api:app --host 0.0.0.0 --port 8000
```

#### doubao-rag-service

```bash
cd doubao-rag
# 检查是否可以独立运行
python -m uvicorn backend.api:app --host 0.0.0.0 --port 8001
```

### 验证 3: 检查主仓库

```bash
# 在主仓库根目录
git status
# 应该显示 clean working tree

# 确认目录已被忽略
git check-ignore jubianai/
git check-ignore doubao-rag/
# 应该返回路径，表示已被忽略
```

## 📝 后续步骤

### 1. Docker 部署准备

如果计划使用 Docker 部署：

1. **jubianai-backend**:
   - 将 `jubianai/Dockerfile.template` 重命名为 `Dockerfile`
   - 将 `jubianai/docker-compose.yml.template` 重命名为 `docker-compose.yml`
   - 配置环境变量

2. **doubao-rag-service**:
   - 将 `doubao-rag/Dockerfile.template` 重命名为 `Dockerfile`
   - 将 `doubao-rag/docker-compose.yml.template` 重命名为 `docker-compose.yml`
   - 配置环境变量

### 2. 更新前端配置

如果后端服务地址有变化，更新前端配置：

```bash
# 在 frontend-nuxt/.env 或 Vercel 环境变量中
BACKEND_URL=https://your-backend-url.com
```

### 3. 文档整理

- [ ] 更新主仓库的 README.md，说明服务已分离
- [ ] 在各自的仓库中添加 README.md
- [ ] 更新部署文档

## 🎉 完成确认

如果所有检查项都通过，恭喜！仓库分离已完成。

现在你可以：
- ✅ 独立开发和部署每个服务
- ✅ 使用 Docker 进行容器化部署
- ✅ 在不同服务器上部署服务
- ✅ 独立管理每个服务的版本和发布

---

**最后更新**: 2025-12-28

