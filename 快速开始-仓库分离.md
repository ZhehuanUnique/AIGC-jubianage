# 快速开始：仓库分离

## 🚀 快速步骤

### 1. 创建 GitHub 仓库

在 GitHub 上创建两个新仓库：
- `jubianai-backend` - 后端服务
- `doubao-rag-service` - RAG 服务

### 2. 分离 jubianai

```bash
# 进入 jubianai 目录
cd jubianai

# 初始化 Git（如果还没有）
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: jubianai backend service"

# 添加远程仓库（使用 SSH，更稳定）
git remote add origin git@github.com:ZhehuanUnique/jubianai-backend.git

# 如果已存在 HTTPS 远程仓库，可以更新为 SSH：
# git remote set-url origin git@github.com:ZhehuanUnique/jubianai-backend.git

# 推送
git branch -M main
git push -u origin main
```

### 3. 分离 doubao-rag

```bash
# 进入 doubao-rag 目录
cd ../doubao-rag

# 初始化 Git
git init

# 添加所有文件
git add .

# 提交
git commit -m "Initial commit: doubao-rag service"

# 添加远程仓库（使用 SSH，更稳定）
git remote add origin git@github.com:ZhehuanUnique/doubao-rag-service.git

# 如果已存在 HTTPS 远程仓库，可以更新为 SSH：
# git remote set-url origin git@github.com:ZhehuanUnique/doubao-rag-service.git

# 推送
git branch -M main
git push -u origin main
```

### 4. 更新主仓库

```bash
# 回到主仓库根目录
cd ../AIGC-jubianage

# 添加到 .gitignore
echo "jubianai/" >> .gitignore
echo "doubao-rag/" >> .gitignore

# 提交更改
git add .gitignore
git commit -m "Remove jubianai and doubao-rag, moved to separate repos"
git push
```

---

## 🐳 Docker 快速部署

### jubianai-backend

1. **创建 Dockerfile**（在 jubianai-backend 根目录）

2. **创建 docker-compose.yml**

3. **启动服务**
   ```bash
   docker-compose up -d
   ```

### doubao-rag-service

1. **创建 Dockerfile**（在 doubao-rag-service 根目录）

2. **创建 docker-compose.yml**

3. **启动服务**
   ```bash
   docker-compose up -d
   ```

详细配置请参考 `仓库分离和部署指南.md`

---

## ⚙️ Vercel 配置（无需更改）

主前端项目已经在 Vercel 上正确配置：
- Root Directory: `frontend-nuxt`
- Framework: Nuxt.js
- 配置文件: `frontend-nuxt/vercel.json`

**无需额外配置**，Vercel 会自动检测并使用现有配置。

---

## 📝 下一步

1. ✅ 完成仓库分离
2. ✅ 测试独立服务运行
3. ✅ 配置 Docker 部署
4. ✅ 更新前端 API 地址（如果需要）

详细说明请查看 `仓库分离和部署指南.md`

