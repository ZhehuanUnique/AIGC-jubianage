# Vercel 部署检查清单

## ✅ 当前状态检查

### 1. 仓库大小
- Git 仓库压缩后: **172.09 MiB** ✅ (小于 250MB)
- 主仓库已清理 poster 文件夹 ✅
- jubianai 和 doubao-rag 已分离 ✅

### 2. Vercel 配置
- ✅ `frontend-nuxt/vercel.json` 配置正确
- ✅ `frontend-nuxt/.vercelignore` 已配置
- ✅ Root Directory 应设置为: `frontend-nuxt`

### 3. 需要检查的项目

#### 视频文件
- ⚠️ 如果 `index.mp4` 和 `index.webm` 还在本地且很大，建议：
  - 已上传到 COS：可以删除本地文件
  - 未上传：需要上传到 COS 或从部署中排除

## 🚀 部署步骤

### 方法 1: 通过 GitHub 自动部署（推荐）

1. **提交当前更改**
   ```bash
   git add .
   git commit -m "Prepare for Vercel deployment"
   git push origin main
   ```

2. **Vercel 会自动检测并部署**
   - 如果已连接 GitHub，推送后会自动触发部署
   - 在 Vercel Dashboard 查看部署状态

### 方法 2: 在 Vercel Dashboard 手动部署

1. **进入 Vercel Dashboard**
   - 访问 https://vercel.com/dashboard
   - 选择项目 `AIGC-jubianage`

2. **检查项目设置**
   - **Root Directory**: `frontend-nuxt`（重要：无尾部斜杠）
   - **Framework Preset**: `Nuxt.js`
   - **Build Command**: `npm run build`（自动检测）
   - **Output Directory**: `.output/public`（自动检测）

3. **手动触发部署**
   - 点击 **Deployments** 标签
   - 点击 **Redeploy** 或创建新部署

## ⚙️ 环境变量配置

在 Vercel Dashboard → Settings → Environment Variables 中确保有：

```env
BACKEND_URL=https://jubianai-backend.onrender.com
```

## 🔍 部署后检查

### 1. 检查部署日志
- 在 Vercel Dashboard 查看构建日志
- 确认没有错误

### 2. 访问网站
- 访问 Vercel 提供的域名
- 检查页面是否正常加载

### 3. 检查功能
- ✅ 页面加载正常
- ✅ API 请求成功
- ✅ 静态资源加载正常

## ⚠️ 可能的问题

### 问题 1: 构建失败

**检查**:
- 构建日志中的错误信息
- Node.js 版本是否兼容
- 依赖是否正确安装

### 问题 2: 文件大小超限

**解决**:
- 确认 `.vercelignore` 正确配置
- 检查是否有大文件被包含
- 使用 COS 存储大文件

### 问题 3: 路由问题

**检查**:
- `vercel.json` 中的路由配置
- Nuxt 路由是否正确

## 📝 部署前检查清单

- [ ] 代码已提交到 GitHub
- [ ] Vercel 项目 Root Directory 设置为 `frontend-nuxt`
- [ ] 环境变量已配置
- [ ] `.vercelignore` 正确配置
- [ ] 大文件已上传到 COS（如果使用）
- [ ] 本地测试构建成功: `cd frontend-nuxt && npm run build`

---

**最后更新**: 2025-12-28

