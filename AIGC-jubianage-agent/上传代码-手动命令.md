# 上传代码到服务器 - 手动命令

## 📋 问题说明

如果 PowerShell 脚本无法执行，可以使用以下手动命令上传代码。

---

## 🚀 方法1：使用 tar 压缩上传（推荐，最快）

### 在 Windows PowerShell 中执行：

```powershell
# 1. 切换到项目目录
cd C:\Users\Administrator\Desktop\AIGC-jubianage-agent

# 2. 压缩代码（排除不需要的文件）
tar --exclude='node_modules' --exclude='.git' --exclude='dist' --exclude='.env*' -czf deploy.tar.gz .

# 3. 上传到服务器
scp deploy.tar.gz ubuntu@119.45.121.152:/tmp/

# 4. 在服务器上解压
ssh ubuntu@119.45.121.152 "cd /var/www/aigc-agent && tar -xzf /tmp/deploy.tar.gz && rm /tmp/deploy.tar.gz"

# 5. 删除本地压缩文件
Remove-Item deploy.tar.gz
```

---

## 🚀 方法2：使用 scp 直接上传（较慢但简单）

### 在 Windows PowerShell 中执行：

```powershell
# 1. 切换到项目目录
cd C:\Users\Administrator\Desktop\AIGC-jubianage-agent

# 2. 上传主要文件
scp package.json package-lock.json ubuntu@119.45.121.152:/var/www/aigc-agent/
scp vite.config.ts tsconfig.json tailwind.config.js postcss.config.js ubuntu@119.45.121.152:/var/www/aigc-agent/
scp index.html ubuntu@119.45.121.152:/var/www/aigc-agent/

# 3. 上传目录
scp -r src ubuntu@119.45.121.152:/var/www/aigc-agent/
scp -r server ubuntu@119.45.121.152:/var/www/aigc-agent/
scp -r public ubuntu@119.45.121.152:/var/www/aigc-agent/
```

---

## 🚀 方法3：使用 WinSCP（图形化工具，最简单）

1. 下载 WinSCP：https://winscp.net/
2. 连接到服务器：
   - 主机名：`119.45.121.152`
   - 用户名：`ubuntu`
   - 密码：`rXTg#!G$6c45yxZ`
3. 上传整个项目文件夹到 `/var/www/aigc-agent/`
4. 排除：`node_modules`、`.git`、`dist`

---

## ✅ 上传完成后验证

在服务器上执行：

```bash
ssh ubuntu@119.45.121.152
cd /var/www/aigc-agent
ls -la
```

应该能看到：
- `package.json`
- `src/` 目录
- `server/` 目录
- 其他项目文件

---

## 📝 下一步

代码上传后，继续执行：

1. 配置环境变量
2. 部署应用
3. 配置 Nginx

详见：`服务器上执行的命令.md`

